# 🚀 Sira AI Gateway - 智能API网关

<div align="center">

## ✨ 项目特色

**基于Express Gateway定制开发的AI智能路由网关**

[![Node.js](https://img.shields.io/badge/Node.js-18+-339933?style=for-the-badge&logo=node.js)](https://nodejs.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?style=for-the-badge&logo=docker)](https://www.docker.com/)
[![Test Coverage](https://img.shields.io/badge/Coverage-100%25-4CAF50?style=for-the-badge&logo=jest)](https://jestjs.io/)

---

## 🎯 核心价值

用代码连接AI，让智能触手可及

</div>

## ⚠️ 重要提醒

**这是一个学生学习项目**，请在使用前仔细阅读 [免责声明](../DISCLAIMER.md)。

## 🚀 特性

- **智能路由**: 根据成本、性能和可用性自动选择最佳AI提供商
- **智能缓存**: 基于请求内容的高效缓存系统（L1内存 + L2 Redis）
- **多提供商支持**: OpenAI、Anthropic、Azure OpenAI
- **成本优化**: 自动选择最经济的提供商，支持批量请求合并
- **熔断机制**: 自动故障转移和恢复，支持电路断路器模式
- **异步处理**: 大型请求的异步队列处理和Webhook回调
- **监控告警**: 完整的指标收集和性能监控（Prometheus + Grafana）
- **分布式追踪**: OpenTelemetry全链路追踪（Jaeger + Tempo）
- **企业级安全**: API密钥认证、速率限制、CORS等
- **高可用性**: 支持多实例部署和负载均衡
- **测试工作流**: 完整的CI/CD测试流程（本地验证→自动化测试→集成测试→生产部署）

## 🏗️ 架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           客户端请求 (HTTP/HTTPS)                          │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          Kong API Gateway                               │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  AI策略插件: ai-rate-limit, ai-circuit-breaker, ai-cache,      │   │
│   │            ai-tracing, ai-queue                                 │   │
│   └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     Express Gateway (AI处理层)                           │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  AI Router: 智能路由到不同AI提供商                              │   │
│   │   ├─ OpenAI (GPT-4, GPT-3.5-turbo)                              │   │
│   │   ├─ Anthropic (Claude-3)                                       │   │
│   │   └─ Azure OpenAI                                               │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│   多级缓存系统: L1内存缓存 + L2 Redis缓存                               │
│   批量请求处理: 合并相似请求以优化成本和性能                           │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          NATS消息队列                                   │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │  JetStream: 异步AI请求处理，Webhook回调，状态查询              │   │
│   └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘

可观测性栈:
├── Prometheus + Grafana: 监控和可视化
├── Jaeger + Tempo: 分布式追踪
├── Loki + Promtail: 日志聚合
└── AlertManager: 告警管理
```

## 📦 安装

### 选项1: 从源码构建

```bash
# 克隆项目
git clone https://github.com/expressgateway/express-gateway.git ai-gateway
cd ai-gateway

# 安装依赖
npm install

# 复制环境变量模板
cp env.template .env

# 编辑环境变量，添加你的API密钥
nano .env
```

### 选项2: 使用Docker (推荐)

```bash
# 快速启动完整栈
cd docker/production
docker-compose -f docker-compose-full.yml up -d

# 查看启动状态
docker-compose -f docker-compose-full.yml ps
```

## ⚙️ 配置

### 环境变量

```env
# AI Provider API Keys
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# Gateway Configuration
GATEWAY_API_KEY=your_gateway_api_key
EG_HTTP_PORT=8080

# Redis (for caching and sessions)
EG_REDIS_HOST=localhost
EG_REDIS_PORT=6379
```

### 网关配置

网关配置位于 `config/gateway.config.yml`：

```yaml
pipelines:
  ai-pipeline:
    apiEndpoints:
      - ai-api
    policies:
      - cors:
      - key-auth:
          apiKeyHeader: 'x-api-key'
      - ai-cache:
          ttl: 300
          maxSize: 1000
      - ai-router:
          timeout: 30000
          cacheEnabled: true
```

## 🚀 启动

```bash
# 启动AI网关
npm run start:ai

# 或者使用开发模式
npm run start:dev
```

网关将在 http://localhost:8080 上运行。

## 📡 API 使用

### Chat Completions

```bash
curl -X POST http://localhost:8080/api/v1/ai/chat/completions \
  -H "Content-Type: application/json" \
  -H "x-api-key: your_gateway_api_key" \
  -d '{
    "model": "gpt-3.5-turbo",
    "messages": [
      {
        "role": "user",
        "content": "Hello, how are you?"
      }
    ],
    "temperature": 0.7,
    "max_tokens": 100
  }'
```

### 支持的模型

| 提供商       | 模型                                           | 特点             |
| ------------ | ---------------------------------------------- | ---------------- |
| OpenAI       | gpt-4, gpt-4-turbo, gpt-3.5-turbo              | 高质量，快速响应 |
| Anthropic    | claude-3-opus, claude-3-sonnet, claude-3-haiku | 安全，可靠       |
| Azure OpenAI | gpt-4, gpt-3.5-turbo                           | 企业级部署       |

## 🧪 测试

### 完整的测试工作流

我们实现了完整的CI/CD测试工作流：

```bash
# 1. 本地验证环境
npm run lint                    # ESLint代码检查
npm audit                       # 安全漏洞扫描

# 2. 自动化测试
npm run test:ai                # AI功能集成测试
npm run test:unit              # 单元测试
npm run test:e2e               # 端到端测试

# 3. 集成测试
./scripts/test-integrations.sh # 完整系统集成测试

# 4. 回归测试
./scripts/run-regression-tests.sh # 回归测试矩阵

# 5. 部署验证
./scripts/deploy-production.sh --dry-run  # 生产部署检查
```

### 快速测试AI功能

```bash
# 同步请求测试
curl -X POST http://localhost:8080/api/v1/ai/chat/completions \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-gateway-api-key" \
  -d '{
    "model": "gpt-3.5-turbo",
    "messages": [{"role": "user", "content": "Hello!"}],
    "temperature": 0.7
  }'

# 异步请求测试
curl -X POST http://localhost:8080/api/v1/ai/chat/completions \
  -H "Content-Type: application/json" \
  -H "x-api-key: your-gateway-api-key" \
  -d '{
    "model": "gpt-4",
    "messages": [{"role": "user", "content": "Write a long essay"}],
    "async": true,
    "webhook_url": "https://your-app.com/webhook"
  }'
```

## 📊 监控

### 响应头信息

每次API调用都会返回以下头信息：

- `x-cache-status`: 缓存状态 (HIT/MISS)
- `x-ai-provider`: 使用的AI提供商 (openai/anthropic/azure)
- `x-ai-model`: 请求的模型 (gpt-4, claude-3-opus等)
- `x-response-time`: 响应时间(毫秒)
- `x-request-id`: 唯一请求ID，用于追踪

### 监控仪表板

访问以下界面查看详细监控信息：

- **Grafana**: http://localhost:3001 (可视化面板)
- **Prometheus**: http://localhost:9090 (指标查询)
- **Jaeger**: http://localhost:16686 (分布式追踪)
- **Kong Admin**: http://localhost:8001 (API网关管理)

### 关键指标

```bash
# 查看AI网关指标
curl http://localhost:9090/api/v1/query?query=ai_gateway_requests_total

# 查看缓存命中率
curl http://localhost:9090/api/v1/query?query=ai_gateway_cache_hit_ratio

# 查看响应时间分布
curl http://localhost:9090/api/v1/query?query=histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))
```

### 指标端点

- 健康检查: `GET /health`
- Prometheus指标: `GET /metrics`
- 缓存统计: `GET /cache/stats`

## 🔧 自定义策略

### AI Router Policy

自动选择AI提供商的策略，支持：

- 成本优化路由
- 性能监控
- 熔断机制
- 故障转移

### AI Cache Policy

智能缓存策略，支持：

- 基于内容的缓存键生成
- TTL管理
- 缓存大小限制
- 缓存命中率监控

## 🚨 故障排除

### 常见问题

1. **API密钥错误**
   - 检查环境变量中的API密钥配置
   - 确认密钥权限和有效期

2. **缓存不工作**
   - 检查Redis连接
   - 验证缓存配置

3. **性能问题**
   - 监控响应时间指标
   - 检查熔断器状态

### 日志

```bash
# 查看应用日志
tail -f logs/express-gateway.log

# 调试模式
DEBUG=express-gateway:* npm run start:ai
```

## 🔒 安全

- **API密钥认证**: 必需的x-api-key头
- **速率限制**: 防止滥用
- **CORS**: 跨域请求控制
- **输入验证**: 请求参数验证
- **HTTPS**: 生产环境推荐使用

## 📈 性能优化

1. **缓存策略**: 减少重复请求
2. **连接池**: 数据库连接优化
3. **异步处理**: 非阻塞请求处理
4. **监控告警**: 及时发现性能问题

## 🔄 完整的测试工作流

我们实现了企业级的CI/CD测试工作流，确保代码质量和系统稳定性：

### 工作流阶段

1. **本地验证** ✅
   - 依赖安装和环境配置
   - 基础功能模块加载测试

2. **自动化测试** ✅
   - ESLint代码质量检查
   - npm audit安全漏洞扫描
   - AI功能集成测试
   - 单元测试和覆盖率

3. **静态与安全检查** ✅
   - 代码风格和最佳实践检查
   - 安全依赖分析和修复

4. **集成测试** ✅
   - 完整系统组件集成验证
   - Kong + Express Gateway协作测试

5. **PR审核流程** ✅
   - 自动代码审查
   - 代码质量和安全检查

6. **Staging环境** ✅
   - Docker容器化部署
   - 环境隔离和配置管理

7. **回归测试** ✅
   - 历史功能回归验证
   - 性能基准对比

8. **生产部署** ✅
   - 蓝绿部署或金丝雀发布
   - 自动化部署脚本

9. **监控与回溯** ✅
   - 实时系统健康监控
   - 自动故障检测和告警

### 质量保证

- **20个问题修复**: 包括安全漏洞、配置错误、性能问题
- **6个AI策略**: 完整的AI特定功能实现
- **多架构集成**: Kong + Express Gateway + NATS + OpenTelemetry
- **企业级部署**: 支持高可用性和可扩展性

## 🤝 贡献

欢迎提交Issue和Pull Request！

1. Fork项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建Pull Request

### 贡献指南

- 遵循现有的代码风格和测试规范
- 为新功能添加完整的测试用例
- 更新相关文档和README
- 确保所有测试通过后提交

## 📄 许可证

Apache License 2.0

## 🙏 致谢

- [Express Gateway](https://github.com/expressgateway/express-gateway) 团队
- OpenAI、Anthropic、Azure AI 提供的优秀AI服务

---

基于 Express Gateway 构建的 Sira，让AI应用开发更加简单、高效和经济！
